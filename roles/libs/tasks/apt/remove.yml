---
# Remove unwanted Software
- name: Remove unwated software
  ansible.builtin.apt: name={{ packages_remove }} state=absent purge=yes autoremove=yes

- name: Remove useless packages from the cache
  ansible.builtin.apt:
    autoclean: yes

- name: Remove dependencies that are no longer required
  ansible.builtin.apt:
    autoremove: yes
